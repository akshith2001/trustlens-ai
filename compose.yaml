services:
  api:
    build: .
    environment:
      TRUSTLENS_API_KEY: ${TRUSTLENS_API_KEY:?Set TRUSTLENS_API_KEY}
    ports:
      - "8501:8501"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
